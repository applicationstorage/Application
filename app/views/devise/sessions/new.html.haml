- content_for :title, 'Log In'

.row
  .col-sm-6.col-sm-offset-3.col-lg-4.col-lg-offset-4
    .panel.panel-default.margin-top-20
      .panel-heading
        %h2.panel-title Log In
      = simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
        .panel-body
          = f.input :username, :placeholder => 'Username', autofocus: true
          = f.input :password, :placeholder => 'Password'
        .panel-footer
          = submit_button 'Log in'
          .margin-top-10= render "devise/shared/links"
          = link_to "Forgot password?", "https://www.sheffield.ac.uk/cics/account/problems", target: "_blank", rel: "nofollow"
